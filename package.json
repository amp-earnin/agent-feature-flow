{
  "name": "feature-flow",
  "private": true,
  "description": "Tooling for the feature-flow Claude Code plugin source repo.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml,js,ts}": "prettier --write"
  }
}
